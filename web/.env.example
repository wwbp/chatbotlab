# API base URL — optional. Leave it unset and the frontend uses a relative
# /api base, which the dev server proxies to the backend and which CloudFront
# proxies in a deployed stack. Set it only to point at an API elsewhere.
VITE_API_URL=http://localhost:8000/api
